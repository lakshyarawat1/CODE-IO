import { AppBar , Container, Toolbar } from '@mui/material'

const Header = () => {
    return (
        <AppBar>
            <Container>
                <Toolbar>
                    
                </Toolbar>
            </Container>
        </AppBar>
    )
}

export default Header